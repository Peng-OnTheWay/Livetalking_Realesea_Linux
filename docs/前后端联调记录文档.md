# 前后端联调记录文档

- **项目名称**：AI数字人导游（灵山胜境智能导览系统）
- **生成时间**：2026-05-07
- **后端端口**：5000（Flask，`app.py` 中 `app.run(port=5000)`）
- **管理端前端端口**：5174（Vite，`admin-frontend/vite.config.js`）
- **游客端前端端口**：5173（Vite，`tourist-frontend/vite.config.js`）

---

## 一、后端已实现接口清单

| 序号 | 方法 | 路径 | 请求参数 | 返回数据示例 |
|------|------|------|----------|-------------|
| 1 | GET | /api/session/new | 无 | `{"session_id": "uuid-string"}` |
| 2 | POST | /api/chat | `{"question":"...", "session_id":"..."}` | `{"answer": "AI 回复内容"}` |
| 3 | POST | /api/feedback/submit | `{"session_id":"...", "star":4, "content":"..."}` | `{"code":200, "msg":"ok"}` |
| 4 | GET | /admin/dashboard/data | 无 | `{"code":200, "data":{"today":N, "week":N, "avg_star":N, "hot":[{"q":"...","cnt":N}]}}` |
| 5 | GET | /admin/report/feeling | 无 | `{"code":200, "data":{"report":"AI 分析报告文本"}}` |
| 6 | GET | /admin/log/list | 无 | `{"code":200, "data":[{"q":"用户问题"}]}` |
| 7 | GET | /admin/feedback/list | 无 | `{"code":200, "data":[{"star":4, "content":"评价内容"}]}` |
| 8 | GET | /admin/doc/list | 无 | `{"code":200, "data":[{"id":N, "name":"文件名", "filename":"文件名"}]}` |
| 9 | POST | /admin/doc/upload | FormData: `file` (仅支持 .docx) | `{"code":200, "msg":"✅ 上传成功"}` |
| 10 | POST | /admin/doc/delete | query: `?id=<doc_id>` | `{"code":200, "msg":"删除成功"}` |
| 11 | GET | /admin/qa/list | 无 | `{"code":200, "data":[{"id":N, "question":"...", "answer":"..."}]}` |
| 12 | POST | /admin/qa/add | `{"question":"...", "answer":"..."}` | `{"code":200, "msg":"✅ 问答对添加成功"}` |
| 13 | POST | /admin/qa/delete | query: `?id=<qa_id>` | `{"code":200, "msg":"删除成功"}` |
| 14 | POST | /api/chat/stream | `{"question":"...", "session_id":"..."}` | SSE 流：`data: {"type":"sentence","text":"..."}` … `data: {"type":"end","emotion":"happy"}` |
| 15 | GET/POST | /api/config/digital-human | GET: `?deviceId=xxx`(选填) / POST: `{model,scale,offsetX,offsetY,deviceId?}` | 全局/设备配置合并返回 |
| 16 | GET/POST | /api/tts/config | GET:无 / POST:`{voice,speed,pitch}` | TTS 配置读写 |
| 17 | POST | /api/device/register | `{deviceId, screenWidth, screenHeight, devicePixelRatio}` | `{"code":200,"msg":"registered"}` |
| 18 | GET | /admin/device/list | 无 | `{"code":200,"data":[{"deviceId":"...","label":"...","screenWidth":N,...}]}` |
| 19 | POST | /admin/device/label | `{deviceId, label}` | `{"code":200,"msg":"ok"}` |
| 20 | POST | /admin/device/delete | `{deviceId}` | `{"code":200,"msg":"ok"}` |
| 21 | GET | /api/map/current | 无 | `{"code":200,"data":{"url":"/images/map_xxx.jpg"}}` |
| 22 | POST | /admin/map/upload | FormData: `file` (jpg/png/gif/webp) | `{"code":200,"msg":"地图上传成功"}` |
| 23 | GET | /admin/map/list | 无 | `{"code":200,"data":[...]}` |

> 注：TTS 使用 subprocess 独立进程合成（绕过 eventlet WebSocket 冲突），v2/v3 优先，失败自动降级 v1。

---

## 二、前端期望接口清单

### 2.1 游客端（tourist-frontend）

| 页面/组件 | 功能说明 | 期望方法和路径 | 期望请求参数和返回结构 |
|-----------|---------|---------------|---------------------|
| App.vue | WebSocket 实时对话 | WS `/ws-api` → 代理到 `ws://127.0.0.1:8080` | 上行：`{v:"1.0", type:"user_input", payload:{text}}`；下行（旧）：`{type:"audio_reply", payload:{text, card, visemes, motion, audioUrl}}`；下行（新）：`{type:"audio_sentence", payload:{text, audioUrl, visemes, motion, card, isFirst, isLast}}` 逐句流式 |
| FloatingLive2D.vue | 口型同步 viseme 字典 | GET `/viseme.json` | 静态 JSON 文件，本地已提供 |

### 2.2 管理端（admin-frontend）

| 页面/组件 | 功能说明 | 期望方法和路径 | 期望请求参数和返回结构 |
|-----------|---------|---------------|---------------------|
| dashboard/index.vue | 数据大屏概览 | GET /admin/dashboard/data | `{code:200, data:{today, week, avg_star, hot}}` |
| analysis/index.vue | 游客感受度报告 | GET /admin/report/feeling | `{code:200, data:{report:"..."}}` |
| analysis/index.vue | 预警反馈列表 | GET /admin/feedback/list | `{code:200, data:[{star, content}]}` |
| brain/index.vue | 文档列表 | GET /admin/doc/list | `{code:200, data:[{id, name, filename}]}` |
| brain/index.vue | 上传文档 | POST /admin/doc/upload | FormData: file (.docx)，返回 `{code:200}` |
| brain/index.vue | 删除文档 | POST /admin/doc/delete?id= | 返回 `{code:200}` |
| brain/index.vue | 问答列表 | GET /admin/qa/list | `{code:200, data:[{id, question, answer}]}` |
| brain/index.vue | 新增问答 | POST /admin/qa/add | `{question, answer}` → `{code:200}` |
| brain/index.vue | 删除问答 | POST /admin/qa/delete?id= | 返回 `{code:200}` |
| brain/index.vue | 历史对话 | GET /admin/log/list | `{code:200, data:[{q}]}` |
| character/index.vue | TTS 语音合成测试 | POST /api/tts | `{text, voice}` → `{status:"success", audioUrl:"..."}` |

---

## 三、接口匹配结果

### ✅ 已匹配接口（前端已接入后端真实接口）

| 后端路径 | 前端调用位置 | 前端功能 | 对接状态 |
|----------|-------------|---------|---------|
| GET /admin/dashboard/data | dashboard/index.vue → fetchDashboardData() | 数据大屏概览统计 | ✅ 已对接 |
| GET /admin/report/feeling | analysis/index.vue → generateReport() | 生成 AI 游客感受度报告 | ✅ 已对接 |
| GET /admin/doc/list | brain/index.vue → fetchDocList() | 知识库文档列表 | ✅ 已对接 |
| POST /admin/doc/upload | brain/index.vue → handleSubmitUpload() | 上传知识文档 (.docx) | ✅ 已对接 |
| POST /admin/doc/delete | brain/index.vue → handleDelete() | 删除知识文档 | ✅ 已对接 |
| GET /admin/qa/list | brain/index.vue → fetchQAList() | 问答对列表 | ✅ 已对接 |
| POST /admin/qa/add | brain/index.vue → handleSubmitQa() | 新增问答对 | ✅ 已对接 |
| POST /admin/qa/delete | brain/index.vue → handleDeleteQa() | 删除问答对 | ✅ 已对接 |
| GET /admin/log/list | brain/index.vue → fetchChatLogs() | 历史对话日志 | ✅ 已对接 |
| GET /admin/feedback/list | analysis/index.vue | 游客反馈列表 | ✅ 已对接 |

### ❌ 前端需要但后端缺失的接口

| 前端功能 | 前端位置 | 期望路径 | 状态 | 处理方式 |
|---------|---------|---------|------|---------|
| — | — | — | — | 当前所有已识别的前端需求均有后端接口覆盖 |

### ⚠️ 后端已实现但前端未使用的接口

| 后端路径 | 功能 | 说明 |
|----------|------|------|
| POST /api/feedback/submit | 提交服务评价 | 仅在后端调试 HTML 中使用，游客端 C 端未集成评价功能 |

---

## 四、联调待办事项

### 4.1 游客端（tourist-frontend）
- **WebSocket 对话**：✅ 已完成。mock-server.js 已改造为 WebSocket-REST 网关
- **TTS 语音合成 + 口型**：✅ 已完成。CosyVoice v2 合成 WAV 音频 + word_timestamp 逐字精确 viseme 口型数据（2026-05-10 升级）
- **打断/静音/语音识别**：✅ 已完成。SpeechRecognition API + mute 按钮 + interrupt 机制
- **口型精度**：✅ 已完成。CosyVoice v2 + word_timestamp_enabled 逐字精确时间戳

### 4.2 管理端（admin-frontend）
- **知识库管理（CRUD）**：✅ 已完成。文档上传/删除/列表、问答对、图片管理
- **TTS 配置**：✅ 已完成。发音人/语速/音调保存到后端，游客端读取
- **数字人形象配置**：✅ 已完成。模型/缩放/位置保存到后端，C 端启动时拉取
- **待办**：数据看板图表数据（见第七章详细分析）

### 4.3 后端
- ✅ 已添加 `flask-cors` 跨域支持
- ✅ 数据库切换为 SQLite（本地开发零配置）
- ✅ 启动自动扫描 `docs/` 和 `images/` 目录
- 待办：补充数据看板时间序列接口

---

## 五、数据看板联调分析

B 端数据看板（`dashboard/index.vue`）包含 4 个模块。当前联调状态如下：

### 5.1 各模块联调状态

| 模块 | 数据来源 | 联调状态 | 说明 |
|------|---------|:--:|------|
| 服务人次趋势（折线/柱状） | `GET /admin/dashboard/traffic?range=daily\|weekly` | ✅ | 2026-05-11 已对接，支持当日 24 小时 / 近 7 天两种粒度 |
| 游客满意度趋势 | `GET /admin/dashboard/satisfaction?days=7` | ✅ | 2026-05-11 已对接，按天计算 AVG(star)*20 |
| 游客关注点词云 | `GET /admin/dashboard/wordcloud?days=7` | ✅ | 2026-05-11 已对接，jieba 分词 + 停用词过滤 TOP50 |
| 热门问题 TOP10 | `GET /admin/dashboard/data` | ✅ | 2026-05-11 增强：satisfaction 和 emotion 改为后端真实返回 |

### 5.2 后端现有接口

当前仅一个聚合接口：

```
GET /admin/dashboard/data
→ { today, week, avg_star, hot: [{ q, cnt }] }
```

| 字段 | 含义 | 来源 |
|------|------|------|
| `today` | 今日独立会话数 | `ChatLog` 表 COUNT DISTINCT session_id |
| `week` | 本周独立会话数 | `ChatLog` 表 COUNT DISTINCT session_id |
| `avg_star` | 平均评价星级 | `Feedback` 表 AVG star |
| `hot` | 热门问答 TOP10 | `ChatLog` 表 GROUP BY question ORDER BY cnt DESC |

### 5.3 后端缺少的数据

要实现图表完全真实驱动，需要后端补充以下接口：

#### 5.3.1 服务人次趋势接口

```
GET /admin/dashboard/traffic?range=daily|weekly
```

**daily 模式返回**（当日按小时统计）：
```json
{
  "code": 200,
  "data": {
    "labels": ["00:00","04:00","08:00","12:00","16:00","20:00","24:00"],
    "values": [20, 15, 120, 350, 480, 200, 95]
  }
}
```

**weekly 模式返回**（本周按天统计）：
```json
{
  "code": 200,
  "data": {
    "labels": ["周一","周二","周三","周四","周五","周六","周日"],
    "values": [850, 930, 900, 1020, 1150, 2300, 2500]
  }
}
```

- **查询逻辑**：从 `ChatLog` 表按时间粒度 GROUP BY 统计独立会话数

#### 5.3.2 游客满意度趋势接口

```
GET /admin/dashboard/satisfaction
```

```json
{
  "code": 200,
  "data": {
    "labels": ["周一","周二","周三","周四","周五","周六","周日"],
    "values": [92, 93, 89, 95, 96, 91, 94]
  }
}
```

- **查询逻辑**：从 `Feedback` 表按天 GROUP BY AVG star 乘以 20 得到百分比

#### 5.3.3 关注点词云接口

```
GET /admin/dashboard/wordcloud
```

```json
{
  "code": 200,
  "data": [
    { "name": "路线规划", "value": 10000 },
    { "name": "门票价格", "value": 8181 },
    { "name": "餐饮服务", "value": 6386 }
  ]
}
```

- **查询逻辑**：对 `ChatLog` 全文或热门问题做关键词提取（或基于已有的 `hot` 数组改造为词云格式）

#### 5.3.4 热门问题 TOP10（增强版）

当前接口已返回 `hot` 数组，但每个问题缺少满意度和情绪。可在现有 `/admin/dashboard/data` 的 `hot` 数组中增加字段：

```json
"hot": [
  { "q": "景区最佳游览路线是什么？", "cnt": 456, "satisfaction": 95, "emotion": "positive" }
]
```

- **查询逻辑**：关联 `Feedback` 表按问题聚合平均星和情绪分布

### 5.4 前端改造要点（后端接口就绪后）

1. **服务人次趋势**：`fetchDashboardData()` 中额外调用 `/admin/dashboard/traffic?range=daily|weekly`，替换 `getChartOption()` 中的硬编码 `data` 数组
2. **满意度趋势**：`onMounted` 中额外调用 `/admin/dashboard/satisfaction`，替换 `initSatisfactionChart()` 的 `data` 数组
3. **词云**：额外调用 `/admin/dashboard/wordcloud`，替换 `initWordCloudChart()` 的 `data` 数组
4. **TOP10**：后端增强 `hot` 字段后，前端移除 `Math.random()` 随机生成满意度的逻辑

---

## 六、VS Code 联调启动指南

### 6.1 架构概览

```
游客端 :5173 ──WebSocket──▶ 网关 :8080 ──REST──▶ 后端 :5000 ──▶ 千问 API
管理端 :5174 ──────REST────▶ 后端 :5000 ──▶ SQLite / Chroma 向量库

启动顺序：① 后端 → ② 网关 → ③ B 端前端 → ④ C 端前端
```

### 6.2 前置准备（仅首次）

```powershell
# 后端 Python 依赖（venv 已装好，仅更新时需跑）
cd his-backend
.\env\Scripts\python.exe -m pip install -r requirements.txt

# 前端 Node 依赖
cd my-frontend\admin-frontend
npm install

cd ..\tourist-frontend
npm install
```

### 6.3 启动步骤

VSCode 打开 `Digital_Human_Project` 目录，**按顺序**开 4 个终端：

| 终端 | 命令 | 端口 | 成功标志 |
|------|------|:--:|------|
| ① 后端 | `cd his-backend` → `.\env\Scripts\python.exe app.py` | 5000 | `🚀 服务启动：全智能版已就绪！` |
| ② 网关 | `cd my-frontend` → `node mock-server.js` | 8080 | `🚀 网关已启动: ws://localhost:8080` |
| ③ B 端 | `cd my-frontend\admin-frontend` → `npm run dev` | 5174 | `Local: http://127.0.0.1:5174/` |
| ④ C 端 | `cd my-frontend\tourist-frontend` → `npm run dev` | 5173 | `Local: http://localhost:5173/` |

### 6.4 验证

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 后端 | 浏览器访问 `http://localhost:5000/api/session/new` | `{"session_id": "xxx"}` |
| B 端 | 访问 `http://localhost:5174` | 页面加载正常，数据看板有数据 |
| C 端 | 访问 `http://localhost:5173`，输入"你好" | 2-3 秒后千问真实回复出现 |

### 6.5 VS Code 断点调试（可选）

`.vscode/launch.json`：

```json
{
  "version": "0.2.0",
  "configurations": [{
    "name": "Python: Flask 后端调试",
    "type": "debugpy",
    "request": "launch",
    "program": "${workspaceFolder}/his-backend/app.py",
    "cwd": "${workspaceFolder}/his-backend",
    "console": "integratedTerminal"
  }]
}
```

`app.py` 设断点 → `F5` → 浏览器触发请求 → 自动停在断点处。前端和网关仍需手动启动。

---

*文档自动生成，每次前后端接口变更后请同步更新。*
